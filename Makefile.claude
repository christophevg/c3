-include .env
-include .env.local

MODEL ?= glm-5:cloud
ARGS ?=

all: claude

claude:
	ollama launch claude --model ${MODEL} -- $(ARGS)

update-claude:
	claude --update

resume: ARGS=--resume
resume: claude
