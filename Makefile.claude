-include .env
-include .env.local

MODEL ?= glm-5:cloud
ARGS  ?=

ifdef TREE
ARGS := $(ARGS) -w $(TREE)
endif

all: claude

claude:
	ollama launch claude --model ${MODEL} -- $(ARGS)

update-claude:
	claude --update

resume: ARGS+=--resume
resume: claude
